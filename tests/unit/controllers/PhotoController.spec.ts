// import PhotoController from '../../../src/controllers/PhotoController';
// import { CREATED, OK } from 'http-status-codes';
// import sinon from 'sinon';

describe('Photo Controller', () => {
	test('', () => {
		expect(1).toBe(1);
	});
	// const sandbox: any = sinon.createSandbox();
	// const mockPhotoRepository: any = { save: sandbox.spy(), find: sandbox.spy() };
	// const controller: PhotoController = new PhotoController({ photoRepository: mockPhotoRepository });

	// afterEach(() => {
	// 	sandbox.restore();
	// });

	// test('Post calls save and returns 201', async () => {
	// 	const mockContext: any = {};
	// 	await controller.post(mockContext);
	// 	expect(mockPhotoRepository.save.callCount).toBe(1);
	// 	expect(mockContext.status).toEqual(CREATED);
	// });

	// test('Get All calls find and returns 200', async () => {
	// 	const mockContext: any = {};
	// 	await controller.getAll(mockContext);
	// 	expect(mockPhotoRepository.find.callCount).toBe(1);
	// 	expect(mockContext.status).toEqual(OK);
	// });
});
